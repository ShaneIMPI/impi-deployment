// Add more regions here later if needed — used by both the Rate Card
// (Officer Roster page) and the event's Region field, so they always
// match exactly.
export const REGIONS = ['Gauteng', 'Cape Town']
